services:
  bot:
    build: .
    container_name: jaded-rose-bot
    env_file: .env
    restart: unless-stopped
    stop_grace_period: 10s
    volumes:
      - bot-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot-data:
